import{useEffect,useState}from"react";import{Activity,BarChart3,Bell,FileText,History as HistoryIcon,Home,ImagePlus,Menu,MessageSquare,ShieldCheck,UploadCloud,Users,X}from"lucide-react";import{analyze,history}from"./api";
type Page="Dashboard"|"New Analysis"|"History";
export default function App(){const[p,setP]=useState<Page>("Dashboard");const[open,setOpen]=useState(false);return <div className="app"><aside className={open?"side open":"side"}><div className="brand"><Activity/> <b>AURA</b><small>Retinal Health AI</small></div><Nav icon={<Home/>} t="Dashboard" active={p==="Dashboard"} go={()=>setP("Dashboard")}/><Nav icon={<ImagePlus/>} t="New Analysis" active={p==="New Analysis"} go={()=>setP("New Analysis")}/><Nav icon={<HistoryIcon/>} t="Analysis History" active={p==="History"} go={()=>setP("History")}/><Nav icon={<MessageSquare/>} t="Messages" go={()=>{}}/><div className="protect"><ShieldCheck/><span><b>Protected health data</b><small>Secure & private</small></span></div></aside><main><header><button className="hamb" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><span>Patient Portal / <b>{p}</b></span><div className="user">TN&nbsp; Tú Nguyễn <Bell size={18}/></div></header><div className="content">{p==="Dashboard"?<Dashboard go={setP}/>:p==="New Analysis"?<NewAnalysis/>:<History/>}</div></main></div>}
function Nav({icon,t,active,go}:{icon:any;t:string;active?:boolean;go:()=>void}){return <button onClick={go} className={active?"nav active":"nav"}>{icon}<span>{t}</span></button>}
function Dashboard({go}:{go:(p:Page)=>void}){return <><section className="hero"><div><small>AI-ASSISTED SCREENING</small><h1>Good evening, Tú.</h1><p>Review your retinal health and keep track of your screening history.</p><button onClick={()=>go("New Analysis")}>+ Start new analysis</button></div><Activity size={100}/></section><div className="stats"><Stat i={<Activity/>}t="Total screenings"v="12"/><Stat i={<BarChart3/>}t="Latest risk score"v="18%"/><Stat i={<FileText/>}t="Reports available"v="9"/><Stat i={<Users/>}t="Doctor consultations"v="3"/></div><section className="panel"><h2>Latest analysis</h2><div className="result"><div className="eye">RETINA</div><div><b>Retinal vascular screening</b><p>18 Aug 2026 · retina_018.jpg</p><span className="low">LOW RISK · 18%</span><p>No major vascular abnormality detected by the screening model.</p></div></div></section></>}
function Stat({i,t,v}:{i:any;t:string;v:string}){return <div className="stat">{i}<span>{t}<b>{v}</b></span></div>}
function NewAnalysis(){const[file,setFile]=useState<File|null>(null);const[result,setResult]=useState<any>(null);const[busy,setBusy]=useState(false);const[err,setErr]=useState("");async function run(){if(!file)return;setBusy(true);setErr("");try{setResult(await analyze(file))}catch(e:any){setErr(e.message)}finally{setBusy(false)}}return <><div className="title"><h1>New retinal analysis</h1><p>Upload a clear fundus image for AI-assisted screening.</p></div><div className="grid"><section className="upload"><UploadCloud size={42}/><h2>{file?file.name:"Upload retinal image"}</h2><p>JPG or PNG · maximum 20 MB</p><input id="f" hidden type="file" accept="image/*" onChange={e=>setFile(e.target.files?.[0]||null)}/><label htmlFor="f">Choose image</label>{file&&<button onClick={run} disabled={busy}>{busy?"Analyzing...":"Analyze image"}</button>}{err&&<div className="error">{err}</div>}</section>{result?<section className="panel"><h2>AI analysis result</h2><div className="risk">{result.riskLevel}<b>{Math.round(result.riskScore*100)}%</b></div><p>{result.explanation}</p><p><b>Model:</b> {result.modelVersion}</p><hr/><b>Findings</b><p>{result.findings?.join(" ")}</p><small>Screening support only — not a medical diagnosis.</small></section>:<section className="panel"><h2>Before you upload</h2><p>✓ Use a clear, focused retinal image</p><p>✓ Ensure the optic disc is visible</p><p>✓ Remove personal information from the image</p><p>✓ Clinical review is recommended</p></section>}</div></>}
function History(){const[data,setData]=useState<any[]>([]);useEffect(()=>{history().then(setData).catch(()=>{})},[]);return <><div className="title"><h1>Analysis history</h1><p>Previous retinal screening results.</p></div><section className="panel"><table><thead><tr><th>Date</th><th>Image</th><th>Risk</th><th>Score</th><th>Model</th></tr></thead><tbody>{data.map(x=><tr><td>{new Date(x.createdAt).toLocaleString()}</td><td>{x.filename}</td><td><span className={x.riskLevel.toLowerCase()}>{x.riskLevel}</span></td><td>{Math.round(x.riskScore*100)}%</td><td>{x.modelVersion}</td></tr>)}</tbody></table>{!data.length&&<p>No analyses yet. Upload your first image.</p>}</section></>}
